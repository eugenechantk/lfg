# LFG release 202609231537

User requested a new TestFlight build and installation on both hosts. Released the current working tree without committing or pushing; retained version train 1.3.0.

## iOS
- Archive/export and upload succeeded with the existing Fastlane signing setup.
- IPA version/build verified as 1.3.0 (202609231537).
- Bundled access configuration contains both Pro and Air hosts.
- Apple verification passed at 2026-09-23 15:44 HKT: `VALID`, highest train `1.3.0`, and `IN_BETA_TESTING`.

## Desktop and host services
| Host | Installed app | Running app PID | New API PID | Handoff route |
|---|---|---:|---:|---|
| Eugenes-MacBook-Pro | /Applications/lfg.app | 97454 | 96900 | 400 for empty request, expected |
| Eugenes-Air | /Applications/lfg.app | 85858 | 84284 | 400 for empty request, expected |

- Both signed binaries have SHA256 `239b3dfe40610e20cf6240f24ca8693b3cd1a90d9bc0769685e55e7913518ef6`.
- Code signatures verified. All 136 desktop headless checks passed on each host.
- Each installed desktop executable reached the other host through its protected HTTPS URL. A deliberately nonexistent source returned the expected handoff-specific error in both directions; no test sessions were created.
- Runtime source files matched byte-for-byte before restarting; no remote source overwrite or Git reset was needed. Air's older Git HEAD accounts for its additional dirty-file list.
- Desktop apps launched successfully through FlowDeck. A generated launcher project points at the installed bundle, since the desktop is normally built with standalone swiftc.
- Existing desktop bundles retained under `~/.lfg/app-backups/lfg-before-202609231537.app` on both hosts.
- Restarted only the API child processes under their existing serve-forever supervisors. No native agent session was deliberately interrupted or closed. All 21 Pro session IDs from before restart remained present; Air's pre-restart API was unreachable, and its post-restart API reports 20 sessions.

## Evidence
`.codex/evidence/release-20260923-model-switch/` contains upload/verification logs, app build and launch logs, source hashes, working-tree manifest, headless checks and API responses. No previously modified source changed during this release.
