# Private build resources

This directory is copied into the LFG app as a folder reference. The local
`BundledCloudflareAccess.private.json` is generated from macOS Keychain and
gitignored; it must never be committed or used in a public build.
