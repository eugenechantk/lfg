# Improvement Log — Session 20260906-121844

## Tracker

- [x] 2026-09-06 — WebFetch on x.com 402'd and claude-in-chrome was not connected; fxtwitter API worked first try (saved as memory)

## Log

### 2026-09-06 — Tweet fetching ladder stalled at both rungs

**What happened:** The CLAUDE.md ladder (WebFetch → claude-in-chrome) failed twice: x.com returned HTTP 402 and the Chrome extension was not connected. `api.fxtwitter.com/<user>/status/<id>` returned the tweet, the quoted tweet and media metadata as JSON on the first try.
**Why this was inefficient:** Both dead rungs cost a round-trip each; the working route was a guess rather than a documented step.
**What better looks like:** Go straight to fxtwitter for any x.com status link. Saved as memory `fetch-tweets-via-fxtwitter`. [done]
