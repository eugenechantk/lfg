# Improvement Log — Session 20260916-162404

## Tracker

- [ ] 2026-09-17 — Spent ~15 min on quick-tunnel throughput experiments before grepping `.claude` for "surfshark"; the 09-10 feature doc and the 09-16 diagnosis already held the whole story

- [ ] 2026-09-17 — Width fix landed on one viewer entry point (transcript cards) and missed the second (Files & Links sheet); a live tap caught it

- [ ] 2026-09-17 — Wrote a "is the bypass alive" probe (dig from inside the bundle) that passes in the exact state it must refuse; only running it in the bad state exposed it

## Log

### 2026-09-17 — Rediscovered the Surfshark/cloudflared story instead of reading it

**What happened:** Measured 90 KB/s through the tunnel and 420 ms edge RTT, then built a four-variant quick-tunnel experiment (http2/quic × VPN/en0) that produced nothing usable (trycloudflare DNS never resolved through the VPN; en0-bound cloudflared can't register). Only afterwards did `grep -rli bypasser .claude` surface `.claude/feature/cloudflared-surfshark-bypass.md` and the 09-16 diagnosis, which already documented the cause, the fix, its regression this morning, and the pending step for Eugene.
**Why this was wrong:** "Ground truth before hypothesizing" was honoured for the numbers but not for the repo's own history. A slow-path finding on this project (Cloudflare, Surfshark, RTT) has been diagnosed several times; the first move after confirming the number should have been a search of `.claude/` and memory for the component names.
**What better looks like:** After the first measurement names a component (Surfshark, cloudflared, tmux…), grep `.claude/*.md`, `.claude/feature/`, and the memory index for it before designing any experiment. Budget: one grep, two minutes.

### 2026-09-17 — Missed the second call site into FileViewerSheet

**What happened:** Added the `w=2400` rendition width where `MediaAttachmentsView` opens `FileViewerSheet`, built green, and only the live sim tap (Files & Links → screenshot) showed the original 9 MB PNG still being requested — `AttachmentsSheet.swift` had its own `resolve(rawPath:)` call.
**Why this was wrong:** The decision ("images open as a rendition") was placed at a call site instead of the boundary. Memory [[enforce-at-the-boundary]] says exactly this; I hadn't grepped for other constructors of the sheet before editing one.
**What better looks like:** Before changing what a view receives, `grep -n 'ViewName('` across the target and put the rule in one helper both sites call (`HostFiles.viewerURL(for:)` now). The live tap through the stub is what caught it — keep that step.

### 2026-09-17 — A guard probe that passes in the bad state

**What happened:** `scripts/cloudflared-restore-bypass.sh` first checked "can a dig inside Cloudflared.app resolve the tunnel edge" — copied from the 09-16 diagnosis, where that probe distinguished a black-holed bypass from a working one. Today Bypasser is OFF, the bundle simply rides the VPN, DNS answers fine, and the guard said ✓. The dry-run happened to be run in exactly that state, which is the only reason it was caught.
**Why this was wrong:** The probe answered "is there a network path" when the question was "is the path the BYPASSED one". Same failure family as [[verify-the-discriminating-case]]: a check must be shown to fail in the state it exists to catch.
**What better looks like:** Before shipping any guard, run it once in the bad state and confirm it refuses. Here that meant comparing the bundle's public exit IP against the VPN exit and the en0 exit — the rewritten probe fails today (VPN exit) and will pass once Bypasser is on.

### 2026-09-17 — Session summary
Diagnosis + feature shipped (media-fast-path). Three tracker items above remain unchecked for `/self-improve`: grep `.claude` before designing experiments; put per-view rules at the boundary (`viewerURL(for:)`); run a guard in the bad state before shipping it. Also noted for follow-up outside this repo: prose turns containing bare absolute paths were not persisted to the Claude Code transcript (two data points this session).
