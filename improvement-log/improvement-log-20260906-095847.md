# Improvement Log — Session 20260906-095847

## Tracker

- [x] 2026-09-06 — (memory + project CLAUDE.md written) Spent ~40 min on hypotheses (shell quoting, update selector, binary path, env) before replaying the dying pane under `remain-on-exit`; the one-line replay printed the root cause
- [ ] 2026-09-06 — Shipped tmux primitives into the server path before exercising them in isolation; the first live SC2 test then failed for a reason that needed bisecting
- [ ] 2026-09-06 — Two zsh glob aborts (`no matches found`) killed multi-step Bash batches mid-way (`*` on an empty dir, `=name` command-path expansion)
- [ ] 2026-09-06 — Pre-existing flaky test: `sessions-resumable-closed.test.ts` "stale lease IS returned as closed" fails in the full run, passes alone

## Log

### 2026-09-06 — Hypotheses before the dead-pane replay

**What happened:** The codex resume pane died silently. I chased shell quoting (zsh `nomatch` on `?`), the update selector, `codexBin()` PATH resolution, the server env, and Syncthing/the Air before running the exact argv under `tmux set -g remain-on-exit on`. That replay printed `failed to deserialize stored thread item … unknown variant completed` immediately: codex 0.153.4 wrote the thread, lfg resumed it with 0.146.0.
**Why this was wrong:** Every hypothesis was a guess at what a process said before it died, when the process could simply be made to say it. ~40 minutes and a dozen probes for one command's worth of evidence.
**What better looks like:** A pane that vanishes = replay its exact argv with the pane kept (remain-on-exit) FIRST, read stderr, then hypothesize. Now in project CLAUDE.md and memory `dead-pane-replay-remain-on-exit`.

### 2026-09-06 — Primitives shipped before being exercised

**What happened:** Wrote `tmuxSetRemainOnExit`/`paneDeadStatus`/`codexPaneFailure` and wired them straight into `resumeClosedSession`, then restarted the server for the live test. The live test returned 200 where 502 was expected; only then did I test the primitives one by one (fine) and bisect env/prompt/binary offline (all fine), and re-run the live test.
**Why this was wrong:** A live server round trip (restart + .env pin + restore) is the most expensive test available; new tmux plumbing should be proven with a 10-second scratch-pane harness first so a live failure can only mean integration.
**What better looks like:** For any new spawn/tmux primitive: `bun -e` harness against a scratch pane that dies on purpose, then the integration.

### 2026-09-06 — zsh glob aborts in Bash batches

**What happened:** `for f in dir/*` on an empty dir and `-t =lfg-84d5b7` (zsh `=cmd` expansion) each aborted a whole batch with `no matches found`, losing the rest of that batch.
**What better looks like:** Quote anything with `=`, `?`, `*`, `[`; use `find` for globs that may be empty.

### 2026-09-06 — Flaky test in the full suite

**What happened:** `bun test`: 740 pass, 1 fail — `listResumable closed flag > a session whose lease has gone stale IS returned as closed` (2.5s). Passes alone. Not in code touched here. Logged, not fixed.
