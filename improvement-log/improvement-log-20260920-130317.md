# Improvement Log — Session 20260920-130317

## Tracker

- [ ] 2026-09-20 — Passed `-S "$U"` to flowdeck despite the memory saying the guard rejects variables
- [ ] 2026-09-20 — The documented plist-seeding recipe is unusable under the FlowDeck guard (simctl launch/terminate blocked, `killall` absent in the sim)
- [ ] 2026-09-20 — First `flowdeck run` on a freshly created sim always fails to launch ("did not return a process handle"); second `--no-build` run works
- [ ] 2026-09-20 — `cd` inside a one-off Bash command silently moved the session cwd away from `ios/`
- [ ] 2026-09-20 — iPad check caught a real bug the iPhone hid (ramp clipped by the sidebar panel)

## Log

### 2026-09-20 — `$U` variable in a flowdeck command
**What happened:** Wrote `-S "$U"`; the guard blocked it. Memory `flowdeck-sim-guard-cwd` says exactly this.
**Why this was wrong:** The memory was in context; I read it and still reached for a shell variable out of habit.
**What better looks like:** Literal UDID in every flowdeck command, one flowdeck call per Bash command.

### 2026-09-20 — plist-seeding recipe vs the guard
**What happened:** Project CLAUDE.md's recipe (terminate → write plist → `simctl spawn killall -9 cfprefsd` → `simctl launch`) can't be followed: the guard blocks `simctl terminate`/`launch`, and `killall` doesn't exist inside the 26.5 sim. The seed was overwritten. Cost ~6 tool calls.
**What better looks like:** On a fresh sim the bundled Cloudflare Access hosts connect on their own after ~60s — just wait. The recipe in CLAUDE.md needs a guard-compatible version (`flowdeck stop <shortId>`, launch by tapping the icon) or a launch-arg host override.

### 2026-09-20 — first launch on a fresh sim fails
**What happened:** On both the iPhone and the iPad, the first `flowdeck run` after sim creation built fine and then failed to launch; `--no-build` right after worked. Looked like a crash from my change for a minute.
**What better looks like:** Expect it; after a fresh-sim run, check `flowdeck apps` and re-run `--no-build` before suspecting the code.

### 2026-09-20 — stray `cd`
**What happened:** `cd <evidence dir> && sips …` moved the persistent cwd; the next `flowdeck run` would have run outside `ios/`.
**What better looks like:** Absolute paths in one-off commands; never `cd`.

### 2026-09-20 — iPad check earned its keep
**What happened:** The fade pinned to the screen top looked right on iPhone; on iPad the sidebar panel clips everything above y≈30, leaving the header in the weak half of the ramp.
**What better looks like:** Keep doing the iPad pass for any chrome change — confirmed the ios/CLAUDE.md rule.
