# Improvement Log — Session 20260922-043628

## Tracker

- [ ] 2026-09-22 — Followed a stale memory hostname for Air→Pro ssh (Tailscale name), one dead call; the `pro` alias was in ~/.ssh/config
- [ ] 2026-09-22 — Wrote a verification recipe into memory (`git diff --stat <anchor>`) before running it; it was wrong and had to be corrected minutes later

- [ ] 2026-09-22 — Two dead corpus-survey calls: `ls -t … | head | xargs` hit "argument list too long", then a glob raced a vanished transcript
- [ ] 2026-09-22 — No per-session state debug endpoint; reconstructed the busy verdict by hand with `bun -e`

## Log

### 2026-09-22 — Two dead corpus-survey calls

**What happened:** Surveying how slash commands appear in transcripts, the first call died on `ls -t ~/.claude/projects/*/*.jsonl` (argument list too long — thousands of files), the second on `os.path.getmtime` of a transcript that vanished between glob and stat.
**Why this was wrong:** Two round-trips for zero information. The projects tree is huge and hot; both failure modes are predictable.
**What better looks like:** Survey `~/.claude/projects` with `find -mtime -N -size +1k` piped to a script that wraps every stat in try/except. Consider a small `scripts/transcript-survey.py` if this comes up a third time.

### 2026-09-22 — No per-session state debug endpoint

**What happened:** `session-state.ts` documents that `StateVerdict.source`/`stalled` are "carried for the state debug endpoint", but `GET /api/sessions/:id/state` is 404. To learn which layer said `running` I imported `sessionTurnState` under `bun -e` against the live transcript.
**Why this was inefficient:** The first question for any "why is this session busy" report is "which layer voted and when"; today that takes source reading plus a hand probe.
**What better looks like:** Either add the endpoint the comments already promise (verdict, source, stalled, hook.at, transcript mtime, paneBusy) or fix the comments. Worth raising at `/self-improve`.

### 2026-09-22 — Stale memory hostname for Air→Pro ssh

**What happened:** `air-ssh-access` said Air→Pro is `ssh eugenechan@eugenes-macbook-pro` (Tailscale). It no longer resolves. The memory itself said "there is probably a `pro` alias — check it first", and there was.
**Why this was inefficient:** One failed ssh round-trip that the memory's own caveat would have avoided had I read it as an instruction rather than a footnote.
**What better looks like:** When a memory hedges ("probably", "check first"), run the check before the hedged command. Memory updated: Air→Pro is `ssh pro`.

### 2026-09-22 — Wrote an unverified verification recipe into memory

**What happened:** While deploying to the Pro I wrote "prove nothing was touched with `git diff --stat <anchor>` (empty)" into `air-ssh-access.md`, then ran it and got 65 files / +3463 — because a `git stash create` anchor taken from a stale synced index builds a tree missing the entries the index did not know about. Corrected the memory in the next call.
**Why this was wrong:** A memory is read as fact by future sessions. Recording a method before it has produced the expected output once is exactly the "should work" I am not allowed to ship in code.
**What better looks like:** Run the check, see the output, THEN write the recipe. Same rule as [[probe-shape-before-scripting]] applied to memory writes.

